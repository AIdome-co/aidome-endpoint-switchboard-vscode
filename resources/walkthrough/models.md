# Exploring Models & Providers

View the models available through your gateway endpoint.

This fetches the model list from your configured endpoint and displays:

- Model IDs (for configuring in assistants)
- Provider information
- Supported capabilities

You can copy model IDs directly from the list to use in your assistants.
